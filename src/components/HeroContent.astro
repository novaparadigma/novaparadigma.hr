<div class="hero-content">
  <h1 class="hero-title">
    Postoji još<br />
    jedan način<br />
    gledanja.
  </h1>

  <span class="hero-divider" aria-hidden="true"></span>

  <div class="hero-copy">
    <p>
      NOvA je škola koja više od četrdeset godina iskustva pretvara u
      suvremen program učenja, istraživanja i provjeravanja vlastitim
      iskustvom.
    </p>

    <p>
      Znanje koje se stoljećima prenosilo od učitelja učeniku danas živi
      kroz školu otvorenu svima koji ga žele živjeti, a ne samo razumjeti.
    </p>
  </div>

  <a class="hero-button" href="/program-i-tehnike">
    <span>Upoznaj program</span>
    <span class="hero-arrow" aria-hidden="true">→</span>
  </a>
</div>

<style>
  .hero-content,
  .hero-content * {
    box-sizing: border-box;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    width: 510px;
  }

  .hero-title {
    margin: 0;

    color: #201e1c;
    font-family: var(--font-serif);
    font-size: 58px;
    font-weight: 500;
    line-height: 0.98;
    letter-spacing: -0.03em;
  }

  .hero-divider {
    display: block;
    width: 41px;
    height: 1px;
    margin: 25px 0 20px;
    background: #aa896e;
  }

  .hero-copy {
    width: 445px;
  }

  .hero-copy p {
    margin: 0 0 17px;
    color: #292725;
    font-size: 15px;
    line-height: 1.7;
  }

  .hero-copy p:last-child {
    margin-bottom: 0;
  }

  .hero-button {
    width: 261px;
    height: 47px;
    margin-top: 25px;
    padding: 0 24px 0 28px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    background: rgba(175, 140, 114, 0.9);
    color: #fff;

    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.035em;
    text-decoration: none;
    text-transform: uppercase;

    transition:
      background-color 220ms ease,
      transform 220ms ease;
  }

  .hero-button:hover {
    background: #987862;
    transform: translateY(-2px);
  }

  .hero-button:focus-visible {
    outline: 2px solid #6e5748;
    outline-offset: 4px;
  }

  .hero-arrow {
    position: relative;
    top: -1px;
    font-size: 23px;
    font-weight: 300;
    line-height: 1;
  }

  @media (max-width: 900px) {
    .hero-content {
      width: min(480px, 100%);
    }

    .hero-title {
      font-size: clamp(49px, 7vw, 58px);
    }

    .hero-copy {
      width: min(430px, 100%);
    }
  }

  @media (max-width: 700px) {
    .hero-content,
    .hero-copy {
      width: 100%;
    }

    .hero-content {
      max-width: 510px;
    }

    .hero-title {
      color: #faf6f0;
      font-size: clamp(43px, 12.2vw, 54px);
      font-weight: 500;
      line-height: 0.98;
      letter-spacing: -0.035em;

      text-shadow:
        0 2px 18px rgba(0, 0, 0, 0.24),
        0 1px 2px rgba(0, 0, 0, 0.32);
    }

    .hero-divider {
      width: 43px;
      margin: 22px 0 19px;
      background: #c79b73;
    }

    .hero-copy {
      max-width: 470px;
    }

    .hero-copy p {
      margin-bottom: 16px;
      color: rgba(255, 251, 246, 0.93);
      font-size: 15px;
      line-height: 1.58;

      text-shadow:
        0 1px 12px rgba(0, 0, 0, 0.42),
        0 1px 2px rgba(0, 0, 0, 0.48);
    }

    .hero-button {
      width: min(275px, 100%);
      height: 50px;
      margin-top: 23px;

      background: rgba(142, 103, 75, 0.94);
    }

    .hero-button:hover {
      background: #694a35;
    }

    .hero-button:focus-visible {
      outline-color: #f2dfcd;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: clamp(40px, 11.5vw, 48px);
    }

    .hero-copy p {
      font-size: 14px;
      line-height: 1.58;
    }

    .hero-button {
      width: min(255px, 100%);
      padding-inline: 22px;
      font-size: 13px;
    }
  }
</style>