<div class="hero-image">
  <img
    src="/images/sections/hero-main.webp"
    alt="Planinski krajolik s knjigom Prah dana, starim kovčežićem i ključem"
    loading="eager"
    fetchpriority="high"
  />

  <div class="hero-gradient" aria-hidden="true"></div>
</div>

<style>
  .hero-image,
  .hero-image * {
    box-sizing: border-box;
  }

  .hero-image {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .hero-image img {
    display: block;
    width: 100%;
    height: 100%;

    object-fit: cover;
    object-position: center center;
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;

    background:
      linear-gradient(
        90deg,
        rgba(246, 242, 234, 0.94) 0%,
        rgba(246, 242, 234, 0.84) 14%,
        rgba(246, 242, 234, 0.62) 26%,
        rgba(246, 242, 234, 0.3) 40%,
        rgba(246, 242, 234, 0.1) 54%,
        rgba(246, 242, 234, 0.02) 64%,
        rgba(246, 242, 234, 0) 72%
      );
  }

  @media (max-width: 700px) {
    .hero-image img {
      /*
       * Il punto focale resta sul lato destro:
       * libro, cofanetto e chiave rimangono visibili.
       */
      object-position: right center;
    }

    .hero-gradient {
      /*
       * Sul mobile il testo diventa chiaro.
       * Il velo scuro protegge soprattutto la zona sinistra,
       * lasciando più leggibili gli oggetti sulla destra.
       */
      background:
        linear-gradient(
          90deg,
          rgba(14, 12, 10, 0.88) 0%,
          rgba(14, 12, 10, 0.76) 42%,
          rgba(14, 12, 10, 0.5) 64%,
          rgba(14, 12, 10, 0.2) 82%,
          rgba(14, 12, 10, 0.08) 100%
        ),
        linear-gradient(
          180deg,
          rgba(14, 12, 10, 0.04) 0%,
          rgba(14, 12, 10, 0.12) 48%,
          rgba(14, 12, 10, 0.42) 100%
        );
    }
  }

  @media (max-width: 480px) {
    .hero-image img {
      object-position: right center;
    }

    .hero-gradient {
      background:
        linear-gradient(
          90deg,
          rgba(13, 11, 9, 0.9) 0%,
          rgba(13, 11, 9, 0.79) 48%,
          rgba(13, 11, 9, 0.55) 70%,
          rgba(13, 11, 9, 0.2) 90%,
          rgba(13, 11, 9, 0.1) 100%
        ),
        linear-gradient(
          180deg,
          rgba(13, 11, 9, 0.03) 0%,
          rgba(13, 11, 9, 0.12) 44%,
          rgba(13, 11, 9, 0.48) 100%
        );
    }
  }
</style>